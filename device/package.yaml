# Toit on-device loader for the porta smoke test.
# TODO(porta-agent): add dependencies during implementation, e.g. the local
# TFTP client at ~/workspaceToit/tftp. The on-device install code (flash-image,
# container_registry) is lifted from ~/workspaceToit/jaguar/src and uses the
# SDK's system.containers (no package dependency). See ../docs/specs.
name: loader
description: On-device loader that pulls a container image over UDP/TFTP and installs it.
